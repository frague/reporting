Python utility for wiki reports creation on the base of jira issues worklogs (issues groupped by version) and .git commits 
for last working day. On the base of issues progress data bar-charts and burn-down diagrams can be built in wiki Chart Macro syntax.

SOAP, xml-rpc and cli are used for interaction with confluence wiki and jira.
